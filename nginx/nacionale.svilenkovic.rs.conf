server {
    listen 80;
    server_name nacionale.svilenkovic.rs;
    include /etc/nginx/snippets/security-headers.conf;

    # Allow ACME challenges for SSL renewal
    location ^~ /.well-known/acme-challenge/ {
        allow all;
        root /home/nacionale.svilenkovic.rs/public_html;
    }

    location / {
        return 301 https://$server_name$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name nacionale.svilenkovic.rs;
    include /etc/nginx/snippets/security-headers.conf;

    # SSL (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/nacionale.svilenkovic.rs/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/nacionale.svilenkovic.rs/privkey.pem;

    root /home/nacionale.svilenkovic.rs/public_html;
    index index.html;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Strict CSP (static Next.js export — no eval, no inline scripts beyond Next runtime)
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' data: https://fonts.gstatic.com; img-src 'self' data: https: blob:; connect-src 'self' https:; media-src 'self'; object-src 'none'; frame-ancestors 'self'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests" always;

    # Static asset caching
    location ~* \.(js|css|png|jpg|jpeg|gif|webp|svg|ico|woff|woff2|ttf|eot|glb|gltf)$ {
        include /etc/nginx/snippets/security-headers-only.conf;
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # PHP API
    location /api/ {
        try_files $uri $uri/ =404;

        location ~ \.php$ {
        include /etc/nginx/snippets/security-headers-only.conf;
            fastcgi_pass unix:/run/php/php8.3-fpm.sock;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_read_timeout 30;
        }
    }

    # Block direct access to config
    location = /api/config.php {
        deny all;
        return 403;
    }

    # Next.js static export — SPA fallback per section
    # Try the exact file first, then directory index, then section fallback
    location /products/ {
        try_files $uri $uri/index.html /products/index.html;
    }

    location /collections/ {
        try_files $uri $uri/index.html /collections/index.html;
    }

    location /cart/ {
        try_files $uri $uri/index.html /cart/index.html;
    }

    location /order/ {
        try_files $uri $uri/index.html /order/index.html;
    }

    # OG image (extensionless static PNG)
    location = /opengraph-image {
        include /etc/nginx/snippets/security-headers-only.conf;
        default_type image/png;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header Content-Disposition "inline";
        try_files /opengraph-image =404;
    }

    # Default
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Deny dotfiles
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    access_log /home/nacionale.svilenkovic.rs/logs/access.log;
    error_log /home/nacionale.svilenkovic.rs/logs/error.log;
}
